# Gemini is now reached through the Supabase Edge Function proxy (lib/gemini/proxyClient.js).
# The Gemini API key lives ONLY as a Supabase secret (GEMINI_API_KEY), never on the client.
# No NEXT_PUBLIC_GEMINI_API_KEY is required anymore.
